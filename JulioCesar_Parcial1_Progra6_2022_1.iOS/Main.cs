using System;
using System.Collections.Generic;
using System.Linq;

using Foundation;
using UIKit;

namespace JulioCesar_Parcial1_Progra6_2022_1.iOS
{
    public class Application
    {
        // This is the main entry point of the application.
        static void Main(string[] args)
        {
            // if you want to use a different Application Delegate class from "AppDelegate"
            // you can specify it here.
            UIApplication.Main(args, null, typeof(AppDelegate));

            //Crear un App en Xamarin Forms que visualice una ventana de login (fake login)
            //y que al presionar el botón de “Ingreso” muestre una vista que se encargue
            //de la captura de los datos relativos al modelo por medio de controles Entry.
            //Además, debe contener un botón para la acción “Guardar Activo”
            //(no ejecuta la acción contra el API) y un botón “Cancelar”
            //que regresa a la ventana de Login. (25 puntos).




        }
    }
}
